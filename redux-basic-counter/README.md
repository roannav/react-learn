This is the code that results from running:

### `npx create-react-app <app-name> --template redux`

... and then I just removed the extra stuff, so only the React and Redux code for a most basic counter is left.

